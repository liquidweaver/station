* Transform sprite updates to a cast
* On new connect
  * request all tiles around player
* On player move
  * calculate calculate all new tiles, request those
* On sprite update
  * If within currently viewable, pass up to client
  * If not currently vieweable, cast unsubscribe sender
